<% if @timeslots.any? %>
<p>
    <b>Join an existing time slot:</b><br/>
    <% @timeslots.each do |ts| %>
       <%= ts[:string] %> | <b><%= link_to 'Join', :action => :join, :time => ts[:time] %></b><br/>
    <% end %>
</p>
<% end %>
