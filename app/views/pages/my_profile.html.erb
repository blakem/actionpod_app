<%= render 'user_link' %>
<%= render 'existing_events'%>
<% if @groups.any? %>
  <% @groups.each do |group| %>
    <% @group = group %>
    <%= render 'group_other_timeslots'%>      
  <% end %>
<% else %>
  You need to join a group
<% end %>
<%= render 'show_conferences'%>
<%= render 'random_tip' %>