<table class="profile" summary="Profile information">
  <tr>
	<td class="main">
	  <h1>
	    <%= gravatar_for @user %>
	    <%= @user.name %>
	  </h1>
	<% if @events.any? %>
	  <ul class="home">
	    <li><b>Your existing time slots:</b></li>
        <% @events.each do |event| %>
    	  <li><i><%= event.name %></i> : <%= event.schedule_str %> | 
                 <%= link_to "Edit", edit_event_path(event) %> |
                 <%= link_to 'Delete', event_path(event), :confirm => 'Are you sure you want to delete this call?', :method => :delete %>				
          </li>
        <% end %>
	  </ul>
	<% else %>
	Below is a list of existing time-slots that other users have scheduled.  Simply <b>click the 'Join'</b> button to add yourself to the call. ;-)
	<% end %>
	<% if @timeslots.any? %>
	  <ul class="home">
	    <li><b>Join an existing time slot:</b></li>
        <% @timeslots.each do |ts| %>
  	       <li><%= ts[:string] %> | <b><%= link_to 'Join', :action => :join, :time => ts[:time] %></b></li>
        <% end %>
	  </ul>
	<% end %>
	  <ul class="home">
	    <li><b><%= link_to "Create a new time slot.", new_event_path %></b></li>
	  </ul>
    </td>
    <td class="sidebar round">
      <% if current_user.admin? %>
      <p><strong><%= link_to "Show existing call times", :action => :callcal %></strong></p>
      <% end %>
      <% if @nextcalls.any? %>
        <p><strong>Your next <%= @nextcalls.count == 1 ? 'call' : "#{@nextcalls.count} calls" %>:</strong><br/>
        <% @nextcalls.each do |call| %>
        <%= call %><br/>
        <% end %>
        </p>
      <% end %>
      </p>
	</td>
  </tr>
</table>