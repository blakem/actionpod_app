<%= form_for(@plan, :url => '/pages/plan') do |f| %>
  <%= pretty_object_error_messages(@plan) %>
  <div class="field">
    <plan>
    <p><%= f.label :body, "Update Your Daily/Weekly Goals:", :class => "round" %><br/>
	(This will be emailed to the other participants before your call)
    <%= f.text_area :body, :class => "round" %></p>
    </plan>
  </div>
  <div class="actions">
    <%= f.submit "Update Goals" %>
  </div>
<% end %>

