<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN" "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html>
  <head>
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
  </head>
  <body>
    <p>
    Hey <%= @user.name %>,
    </p>
    <p>&nbsp;</p>

    <p>
    <%= @sender.name %> has added you to the group: <%= @pool.name %>.  
    </p>
    <p>&nbsp;</p>

    <% unless @pool.description.blank? %>
      <p>
       The <%= @pool.name_plus_group %> is described as:<br/>
       <%= simple_format(h(@pool.description.strip)).gsub(/<(br \/|p)>(\s+)/) { "<#{$1}>#{'&nbsp;' * $2.length}" } %>
      </p>
      <p>&nbsp;</p>
    <% end %>

    <% unless @personal_message.blank? %>
      <p>
       <%= @sender.first_name %> included the personal message:<br/>
       <%= simple_format(h(@personal_message.strip)).gsub(/<(br \/|p)>(\s+)/) { "<#{$1}>#{'&nbsp;' * $2.length}" } %>
      </p>
      <p>&nbsp;</p>
    <% end %>

    <p>
    Thanks, and enjoy. ;-) 
    </p>
    <p>&nbsp;</p>

    <p>
    -Blake 
    </p>
    <p>&nbsp;</p>

  </body>
</html>